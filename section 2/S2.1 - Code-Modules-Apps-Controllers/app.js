// myApp will be the only global variable (an object) we're going to create
var myApp = angular.module('myApp', []);

// module is a function that takes the name of your app, and an array of dependencies

// controller

myApp.controller('mainController', function(){

});
